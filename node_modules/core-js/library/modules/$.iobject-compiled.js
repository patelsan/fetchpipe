// indexed object, fallback for non-array-like ES3 strings
'use strict';

var cof = require('./$.cof');
module.exports = 0 in Object('z') ? Object : function (it) {
  return cof(it) == 'String' ? it.split('') : Object(it);
};

//# sourceMappingURL=$.iobject-compiled.js.map