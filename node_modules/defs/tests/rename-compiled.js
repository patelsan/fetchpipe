"use strict";
var x = "x";
function named_fn(a, b) {
    if (true) {
        (function () {
            console.log(x);
        })();
    }

    // let x must be renamed or else it will shadow the reference on line 5
    for (var _x = 0; _x < 2; _x++) {
        console.log(_x);
    }
}
named_fn();

//# sourceMappingURL=rename-compiled.js.map